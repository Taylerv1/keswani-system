"use client";

import React from "react";
import styles from "./SecondarySidebar.module.css";

/**
 * Placeholder for the secondary sidebar.
 * This component will be populated with context-specific
 * navigation as modules (Rent, Electricity, etc.) are built.
 */
export default function SecondarySidebar() {
    return (
        <aside className={styles.secondarySidebar}>
            {/* Placeholder — content will be injected per module */}
        </aside>
    );
}
