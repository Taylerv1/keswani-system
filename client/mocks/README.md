# Mocks

Place mock data files here matching API endpoint paths.

Example: for endpoint `/users`, create `users.ts` exporting default mock data.

```ts
// mocks/users.ts
const mockUsers = [
  { id: 1, name: "John Doe", email: "john@example.com" },
];
export default mockUsers;
```
