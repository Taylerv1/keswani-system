/**
 * Mocks barrel file.
 * Import and re-export mock data modules here as they are created.
 *
 * Example future usage:
 *   export { mockRentData } from "./rent";
 *   export { mockElectricityData } from "./electricity";
 */

export { };
